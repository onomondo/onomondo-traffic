{
  "name": "onomondo-js-template",
  "version": "1.0.0",
  "description": "Template repo for JS projects",
  "engines": {
    "node": "^14.8.0"
  },
  "scripts": {
    "start": "RELEASE=`git rev-parse HEAD` nodemon index.js",
    "start-production": "RELEASE=`git rev-parse HEAD` node index.js",
    "lint": "eslint --ignore-path .gitignore .",
    "test": "yarn lint"
  },
  "devDependencies": {
    "eslint": "^7.14.0",
    "eslint-config-standard": "^16.0.2",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^5.0.0",
    "nodemon": "^2.0.6"
  },
  "dependencies": {
    "load-environment": "^3.0.0"
  }
}
